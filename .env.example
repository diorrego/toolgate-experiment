# Optional, paid live selection. Deterministic tests do not require this key.
TYPESAFE_AI_API_KEY=
# Existing authenticated Codex access is configured by the operator, outside Git.
# Provider OAuth, service keys and database credentials must never be committed.
