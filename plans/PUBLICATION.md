# Publication plan and handoff

User-authorized target: `git@github.com:diorrego/toolgate-experiment.git`.
The remote was verified empty before initialization. Publish the checked source
tree on main with English commit messages. No license is declared.

The README contains exactly four architecture diagrams: direct MCP control,
agent-initiated Toolgate preparation, host-first single Choice, and host-first
parallel binary suitability. All text, labels, prompts and reports are English.
The diagrams preserve the provider authentication boundary, alternative Go/Rust
cores, remote execution decision and authorized local handlers.

Measurements and existing verification records were produced before Git
initialization. Their statements about absent Git commits describe that historical
laboratory state. Publication does not invent a measurement commit: retained file,
configuration, corpus and binary digests identify the executed artifacts.

Before staging, validate diagram syntax/rendering, local links, numerical reports,
English prose and forbidden punctuation. Scan checked files for known credentials,
bound private identifiers and private-key/token patterns. Compare the staged tree
with the publication allowlist, excluding provider source, private state, generated
builds, dependencies and private answers. Sanitized verification logs are included.

Use the existing configured Git author. Initialize only this folder, create an
English commit, push main to the explicit remote, and verify the remote branch
hash against local HEAD. Do not deploy, publish npm packages, add a license or
alter the surrounding laboratory/worktrees. Final commit identity and push status
are reported after the remote verification, not fabricated in advance.

## Prepared and verified

Four Mermaid 11.12.0 sequence diagrams passed parsing and browser rendering.
The source-only copy passed ten Python tests, eleven Node tests and numerical
recomputation of both matrices. Publication checks found no known credentials,
bound private IDs, private-key/JWT patterns, author-specific paths or forbidden
punctuation. The staged tree is checked against the source allowlist. Evidence:
[diagram rendering](../verification/readme-diagrams.json) and
[publication checks](../verification/publication-check.json). No new runtime
dependency was added for diagram validation.
